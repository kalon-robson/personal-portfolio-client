/* eslint-disable sort-keys-fix/sort-keys-fix */
import { base } from '../base';

export const spacing = {
  xSmall: base(0.5),
  small: base(),
  medium: base(2),
  large: base(4),
  xLarge: base(8),
};
